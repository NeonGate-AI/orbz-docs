# Spec prompt

Use `.agents/specs/template.md`. Write observable behavior, constraints, failure behavior, validation/evidence and explicit out-of-scope work. Keep implementation details only when they materially constrain the contract.
