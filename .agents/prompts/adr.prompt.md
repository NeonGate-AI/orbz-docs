# ADR prompt

Create an ADR only for a consequential, long-lived trade-off. Use `.agents/adrs/template.md`, cite the forcing context, state one decision, consequences and credible rejected alternatives. Do not use an ADR for task planning.
