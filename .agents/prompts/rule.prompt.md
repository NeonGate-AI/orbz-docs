# Rule prompt

Create a durable rule only when a constraint should apply beyond one spec. Keep it small, testable where possible, assign the next stable numeric ID and update `.agents/rules/readme.md`.
